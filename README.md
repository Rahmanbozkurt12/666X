# 666X  
The official repository for the 666X meme token project

## Multichain → Binance confluence bot

```bash
pip install -r requirements.txt
python3 -m binance_confluence --once
```

Details: `binance_confluence/README.md`
Config: `config/binance_confluence.json`
